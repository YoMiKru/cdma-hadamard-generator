[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hadamard-cdma-generator"
version = "0.1.0"
description = "Générateur de matrices de Hadamard pour systèmes CDMA"
requires-python = ">=3.8"
dependencies = ["numpy"]